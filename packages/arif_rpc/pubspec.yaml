name: arif_rpc
description: aria2 / aria2-next JSON-RPC client for arif
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  http: ^1.2.2
  web_socket_channel: ^3.0.1
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
