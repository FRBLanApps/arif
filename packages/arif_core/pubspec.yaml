name: arif_core
description: Domain models and connection profiles for arif
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  arif_rpc:
    path: ../arif_rpc
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
